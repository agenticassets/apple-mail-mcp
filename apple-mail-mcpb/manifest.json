{
  "manifest_version": "0.3",
  "name": "Apple Mail MCP",
  "version": "3.12.1",
  "description": "Natural language interface for Apple Mail and Apple Calendar with 41 tools: email search, composition, bulk operations, smart inbox analytics, folder management, taxonomy, archiving, drafting, draft verification, attachments, Mail rules advising, bounded export, security-hardened destructive operations, bounded calendar reads, conflict-checked event creation, availability search, and gated calendar management. Bundled Claude Code workflow skills plus Interactive UI Dashboard.",
  "author": {
    "name": "Agentic Assets"
  },
  "license": "MIT",
  "homepage": "https://github.com/Agentic-Assets/apple-mail-mcp",
  "repository": {
    "type": "git",
    "url": "https://github.com/Agentic-Assets/apple-mail-mcp.git"
  },
  "documentation": "https://github.com/Agentic-Assets/apple-mail-mcp#readme",
  "support": "https://github.com/Agentic-Assets/apple-mail-mcp/issues",
  "privacy_policies": [
    "https://github.com/Agentic-Assets/apple-mail-mcp/blob/main/PRIVACY.md"
  ],
  "compatibility": {
    "platforms": [
      "darwin"
    ],
    "runtimes": {
      "python": ">=3.13,<3.14"
    }
  },
  "user_config": {
    "user_preferences": {
      "type": "string",
      "title": "Email Preferences",
      "description": "Your personal email preferences that will be provided to the AI assistant when using email tools (e.g., 'Default to BCG account, show max 50 emails, prefer Archive folder')",
      "required": false,
      "default": ""
    },
    "default_account": {
      "type": "string",
      "title": "Default Mail Account",
      "description": "When set, email tools default to this account instead of scanning all configured Mail accounts. Use the exact account name as it appears in Apple Mail (e.g. 'Gmail', 'Work', 'iCloud'). Leave blank to query all accounts by default.",
      "required": false,
      "default": ""
    },
    "default_signature": {
      "type": "string",
      "title": "Default Mail Signature",
      "description": "Optional Apple Mail signature name to apply by default to compose, reply, and forward drafts (e.g. 'TU'). Leave blank to use Mail's existing behavior.",
      "required": false,
      "default": ""
    }
  },
  "server": {
    "type": "python",
    "entry_point": "apple_mail_mcp.py",
    "mcp_config": {
      "command": "/bin/bash",
      "args": [
        "${__dirname}/start_mcp.sh",
        "--draft-safe"
      ],
      "env": {
        "USER_EMAIL_PREFERENCES": "${user_config.user_preferences}",
        "DEFAULT_MAIL_ACCOUNT": "${user_config.default_account}",
        "DEFAULT_MAIL_SIGNATURE": "${user_config.default_signature}"
      }
    }
  },
  "tools": [
    {
      "name": "get_inbox_overview",
      "description": "Dashboard overview across all accounts: unread counts, mailbox structure with unread indicators, recent-email preview, and AI-driven action suggestions."
    },
    {
      "name": "list_inbox_emails",
      "description": "List inbox emails across all accounts or a single account. Defaults to 50 most-recent; a single call scans at most 50 messages. max_emails=0 is rejected with UNBOUNDED_SCAN_REQUIRED (full-mailbox walks are disabled; pass a bounded max_emails or page with offset). Canonical params are max_emails and include_read (legacy aliases `limit` / `unread_only` are accepted with a warning). include_read=False binds a bounded newest-first slice before filtering, so Mail never materializes a large Exchange inbox. Mail calls are serialized, so never issue parallel Mail tool calls; returns {emails, errors, warnings} JSON."
    },
    {
      "name": "get_mailbox_unread_counts",
      "description": "Per-mailbox or per-account unread counts. Pass summary_only=True for the fast per-account roll-up."
    },
    {
      "name": "list_accounts",
      "description": "List all Mail accounts configured in Apple Mail."
    },
    {
      "name": "list_account_addresses",
      "description": "List the sender email addresses (aliases) configured for a given Mail account."
    },
    {
      "name": "list_mailboxes",
      "description": "List mailboxes (folders) for one account or all accounts, with optional message counts."
    },
    {
      "name": "move_email",
      "description": "Move emails by message_ids (preferred, fast). allow_filter_scan=true plus older_than_days is the only filter escape hatch for date/bulk moves; subject_keyword and sender are schema-compat only and always return TARGET_SELECTOR_DEPRECATED even with allow_filter_scan=true. Nested mailbox paths use '/'. Default max 50; dry_run=true previews ID moves."
    },
    {
      "name": "reply_to_email",
      "description": "Reply by message_id (required \u2014 discover via search_emails or list_inbox_emails). subject_keyword on this tool is schema-compat only and returns TARGET_SELECTOR_DEPRECATED. Default native_format=true composes in Mail's native reply window (keeps colored quote bar and logo signature); reply_body is typed above the source-attributed quote and needs Mail window focus plus Accessibility permission for the host, or returns REPLY_WINDOW_FOCUS_FAILED. Draft/open mode verifies the full body, source-attributed quote, and every requested attachment's filename/count/readability. An RFC-backed identity may authorize later guarded retype after revalidation; a blank-iCloud-RFC transaction identity verifies only this call and is not a reusable cleanup handle. Attachment-bearing replies cannot use mode=send: save, verify, inspect, then send explicitly after approval. native_format=false is the windowless object-model fallback (plain-text quote). Default mode saves to Drafts and mode=open leaves the saved compose window open for review. Supports DEFAULT_MAIL_SIGNATURE, signature_name, include_signature, reply-all, and CC/BCC. body_html is accepted for backward compatibility but ignored for native replies."
    },
    {
      "name": "compose_email",
      "description": "Compose a new standalone email from a specific account. Default mode saves quietly to Drafts; mode=open saves first then leaves the window for review. Attachment-bearing calls reject mode=send and become ready only after immediate strict readback of recipient, subject, body, requested filename/count, readable sizes, and warnings at one transaction-scoped Drafts locator. Numeric Draft IDs are best-effort locators. Does not include original thread context and refuses reply-like Re:/Fwd: drafts unless standalone_confirmed=True. Supports DEFAULT_MAIL_SIGNATURE, signature_name, include_signature, TO/CC/BCC, attachments, and optional HTML body. Bare https:// URLs on their own line in HTML compose may become Mail link-preview cards in the open window; the tool does not create or verify those cards."
    },
    {
      "name": "forward_email",
      "description": "Forward by message_id (required \u2014 discover via search_emails or list_inbox_emails). subject_keyword is schema-compat only and returns TARGET_SELECTOR_DEPRECATED. Attach only explicit local paths; source-message attachments are never copied implicitly. Attachment-bearing calls reject mode=send and become ready only after same-operation marker proof or immediate strict readback confirms recipients, body, requested filename/count, and readability. A numeric Drafts locator is optional and volatile; marker proof does not make it reusable. Default mode saves quietly to Drafts; mode=open saves first then leaves the window for review. Supports DEFAULT_MAIL_SIGNATURE, signature_name, include_signature, optional prepended message, CC/BCC, and attachments."
    },
    {
      "name": "create_rich_email_draft",
      "description": "Build a standalone rich HTML multipart .eml with optional explicit local attachments. open_in_mail=false creates a prepared EML only, not a Mail-verified draft. open_in_mail=true preserves the EML and delegates to the focused HTML compose_email transaction, returning immediate strict readback or RICH_DRAFT_COMPOSE_FAILED. Does not include original thread context and refuses reply-like Re:/Fwd: drafts unless standalone_confirmed=True. Prefer over injecting raw HTML into AppleScript content. Bare https:// URLs on their own line in HTML compose may become Mail link-preview cards in the open window; the tool does not create or verify those cards."
    },
    {
      "name": "manage_drafts",
      "description": "Complete draft lifecycle: list, find by thread header, create, send, open, delete. action=list returns Drafts ids; action=find scans bounded newest Drafts headers for In-Reply-To or References; send/open/delete require exact draft_id (draft_subject is schema-compat only and returns TARGET_SELECTOR_DEPRECATED). Guarded deletion is optional; when used, expected_in_reply_to, expected_subject, and expected_to are all required and re-read immediately before deletion. action=create makes a standalone draft and refuses reply-like Re:/Fwd: drafts unless standalone_confirmed=True. Send action is blocked in --read-only and --draft-safe mode."
    },
    {
      "name": "verify_draft",
      "description": "Verify one exact Apple Mail Drafts message id. Returns JSON for recipients, subject, body sentinel match, attachment names and sizes, signature detection above the quote, quoted-original status, In-Reply-To and References headers, and mismatch warnings."
    },
    {
      "name": "verify_drafts",
      "description": "Verify multiple exact Apple Mail Drafts message ids and merge the per-draft JSON snapshots. Uses exact draft ids only and reports missing or invalid ids."
    },
    {
      "name": "list_email_attachments",
      "description": "List attachments by message_ids (required \u2014 discover via search_emails or list_inbox_emails). subject_keyword is schema-compat only and returns TARGET_SELECTOR_DEPRECATED. Capped at max_results (default 50) for fast responses on large inboxes."
    },
    {
      "name": "save_email_attachment",
      "description": "Save a specific attachment to disk. Requires message_ids from prior search_emails, list_inbox_emails, or list_email_attachments; use list_email_attachments to pick attachment_index. Validates target path \u2014 blocks writes to sensitive directories."
    },
    {
      "name": "search_emails",
      "description": "Unified email search across 16 filter parameters: subject, body, sender, date range, attachments, flagged, newsletter detection, threads. Defaults to last 48 hours and the default account (DEFAULT_MAIL_ACCOUNT). recent_days=0 without date_from is rejected with UNBOUNDED_SCAN_REQUIRED; pass recent_days=7 or date_from='YYYY-MM-DD' (full-mailbox sweeps are disabled). all_accounts=True overrides default account. body_text requires allow_body_scan=True or returns BODY_SCAN_DISABLED. A single call scans at most 50 messages (hard ceiling) regardless of window, so it stays fast on large inboxes."
    },
    {
      "name": "get_email_by_id",
      "description": "Fetch one exact email by the Apple Mail message id returned from search results. Use after search_emails when you need full body or stable metadata without another broad subject search."
    },
    {
      "name": "get_email_by_ids",
      "description": "Fetch multiple exact emails by reviewed Apple Mail message ids, chunked internally to avoid long AppleScript predicates. Returns JSON with items, missing_ids, and invalid_ids."
    },
    {
      "name": "get_email_thread",
      "description": "Return the full conversation thread for an email, grouped by subject across Inbox + Sent. Prefer message_id from search_emails or list_inbox_emails; subject_keyword is optional when message_id is set. Defaults to last 48 hours; recent_days=0 is rejected with UNBOUNDED_SCAN_REQUIRED (pass recent_days=7 or smaller; full-mailbox sweeps are disabled)."
    },
    {
      "name": "update_email_status",
      "description": "Batch mark read/unread, flag/unflag by message_ids (preferred). allow_filter_scan=true plus older_than_days or apply_to_all is the only filter escape hatch; subject_keyword and sender are schema-compat only and always return TARGET_SELECTOR_DEPRECATED. Default max 10 per call."
    },
    {
      "name": "manage_trash",
      "description": "Trash lifecycle: move_to_trash, delete_permanent, empty_trash. Prefer message_ids; allow_filter_scan=true plus older_than_days only for date/bulk paths \u2014 subject_keyword and sender always return TARGET_SELECTOR_DEPRECATED. empty_trash requires confirm_empty=True. dry_run defaults to true and only previews \u2014 pass dry_run=false to actually delete. Default max 5."
    },
    {
      "name": "create_mailbox",
      "description": "Create a new mailbox (folder) in an account. Supports nested paths and auto-creates parents."
    },
    {
      "name": "synchronize_account",
      "description": "Explicitly confirmed Mail.app sync for a single account or all accounts. Requires confirm_sync=True because syncing may fetch a large remote backlog."
    },
    {
      "name": "get_awaiting_reply",
      "description": "Sent emails that haven't received a reply. Cross-references Sent vs Inbox over a configurable window (default 7 days)."
    },
    {
      "name": "get_needs_response",
      "description": "Unread emails that likely need a response. Filters out newsletters/automated senders, assigns priority hints, and excludes already-replied and already-drafted mail by default."
    },
    {
      "name": "get_top_senders",
      "description": "Rank senders by frequency over a date window (default 30 days). days_back=0 is rejected with UNBOUNDED_SCAN_REQUIRED (pass days_back=7 or 30; full-mailbox sweeps are disabled). Optional grouping by domain. Top-N sort happens Python-side for fast responses."
    },
    {
      "name": "get_statistics",
      "description": "Email analytics: account overview, sender stats, or mailbox breakdown. Short windows fan across 10 mailboxes, longer windows across 20, each capped at 50 messages per mailbox. days_back=0 is rejected with UNBOUNDED_SCAN_REQUIRED (pass days_back=7 or 30; full-mailbox sweeps are disabled) to prevent unbounded scans on large inboxes."
    },
    {
      "name": "export_emails",
      "description": "Export emails to txt, html, or raw RFC 822 eml, hard-capped at 50 emails per call. Prefer exact message_ids for reviewed batches (a message_ids list over 50 is rejected before Mail.app runs). single_email requires message_id; filtered, correspondent, and thread scopes discover bounded message ids first; correspondent includes Sent by default for back-and-forth history. entire_mailbox exports a bounded, deterministically newest-first page (default 25) from a mailbox and reports each exported message_id. EML can include attachments, with 25 MiB per-file and 100 MiB per-batch caps. Unsupported formats, including pdf, fail before Mail.app runs."
    },
    {
      "name": "full_inbox_export",
      "description": "Disabled: returns UNBOUNDED_EXPORT_DISABLED. Full-mailbox walks are no longer supported because they overload Mail.app on large inboxes. Use the bounded tools export_emails, list_inbox_emails, or search_emails instead. The tool stays registered so existing configs keep loading."
    },
    {
      "name": "inbox_dashboard",
      "description": "Interactive UI dashboard for MCP Apps-compatible clients (requires mcp-ui-server). Account cards, recent emails, and search."
    },
    {
      "name": "list_calendars",
      "description": "List every calendar with id, writability, defaults, and engine diagnostics"
    },
    {
      "name": "list_events",
      "description": "List events in a bounded window with title/query and participant name/email filters, recurring expansion, and paging. query and participant_query are conjunctive when both are supplied. EventKit also searches organizer metadata; Calendar.app scripting does not, and attendee metadata is not included in list output."
    },
    {
      "name": "get_events_by_id",
      "description": "Fetch full detail (notes, alarms, attendees) for exact event ids, window-bounded"
    },
    {
      "name": "check_availability",
      "description": "Busy blocks and free slots folded in Python over a bounded window (max 62 days)"
    },
    {
      "name": "create_event",
      "description": "Create one event with timezone-correct times, alarms, recurrence, and conflict detection"
    },
    {
      "name": "batch_create_events",
      "description": "Create up to 25 one-off events on one calendar with all-or-nothing validation"
    },
    {
      "name": "update_event",
      "description": "ID-first PATCH update with recurring span rules and attendee-set diffing"
    },
    {
      "name": "delete_events",
      "description": "Exact-id bulk delete with dry-run default, caps, and whole-series span confirmation"
    },
    {
      "name": "manage_calendars",
      "description": "Create, rename, or delete calendars; delete is triple-gated with a cascade preview"
    },
    {
      "name": "respond_to_invitation",
      "description": "Documented refusal: no public macOS API can RSVP to invitations"
    }
  ],
  "prompts": []
}
