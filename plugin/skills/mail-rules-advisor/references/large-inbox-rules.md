# Large-inbox pre-flight (canonical)

Canonical source: `plugin/skills/references/large-inbox-rules.md`. After edits, run `python3 tools/validators/sync_skill_references.py` to refresh per-skill copies under `*/references/`.

Apple Mail's AppleScript bridge slows non-linearly on large mailboxes (24k+ is common). Before running any discovery or bulk tool:

1. **Size the inbox once per session**: `get_inbox_overview(output_format="compact", include_mailboxes=false, include_recent=false)`. If it returns slowly or partially, treat the inbox as large and apply the rules below.
2. **Bound every scan; expect a hard per-call ceiling.** Pass an explicit `recent_days` (start at 2, widen only on demand). Tools refuse unbounded scans by default: `recent_days=0` and `max_emails=0` return a structured `UNBOUNDED_SCAN_REQUIRED` error. `search_emails` and `list_inbox_emails` also scan at most **50 messages per call** regardless of `limit` / `recent_days` / window size, so no single call ever walks a whole mailbox. `full_inbox_export` is **disabled** (`UNBOUNDED_EXPORT_DISABLED`) and is never the answer for "every message": page with `offset` across bounded `search_emails` / `list_inbox_emails` calls, or use `export_emails` (also capped at 50 per call; page further with `offset`).
3. **Discovery scans stay bounded; mutations stay ID-first.** Prefer `search_emails(sender_exact=...)` or `search_emails(sender_domain=...)` when possible; fuzzy `sender=...` is discovery-only and should stay bounded with a tight `recent_days` ceiling. For attachments, search with `has_attachments=True` first, then call `list_email_attachments(message_ids=[...], mailboxes=[...])`, naming the mailboxes those ids came from: the default searches the inbox only, and an id held elsewhere comes back in `unresolved_message_ids` rather than as "no attachments". For `move_email`, `update_email_status`, and `manage_trash`, collect `message_ids` from a bounded `search_emails` / `list_inbox_emails` pass and pass `message_ids=[...]` (see rule 8).
4. **`get_awaiting_reply` is timeout-prone** (it cross-walks Sent mail). Start with `days_back=2, max_results=5`; if it stalls, skip it and check Sent directly with `search_emails(mailbox="Sent", recent_days=2, ...)`.
5. **Always drill by id, never re-search by subject.** Once `search_emails` / `list_inbox_emails` returns a `message_id`, use `get_email_by_id(account=..., message_id=...)` and `get_email_thread(account=..., message_id=...)`. Re-searching by subject re-pays the scan cost.
6. **Param names matter.** `list_inbox_emails` takes `max_emails` (not `limit`) and `read_status` ∈ `"all"`/`"unread"`/`"read"`; matches `search_emails`. Example: `list_inbox_emails(max_emails=25, read_status="unread", include_content=False)`. The legacy `include_read=False` / `unread_only=True` still work but emit a deprecation warning. **Pass `date_to` only together with `date_from`.** An omitted `date_from` is filled from `recent_days` (default 2.0), so a bare past `date_to` builds a window that ends before it starts and returns an empty result rather than an error.
7. **`inbox_dashboard` is the rescue path.** When `get_inbox_overview` times out or returns partial JSON, fall back to `inbox_dashboard(output_format="json")` — the default `output_format="ui"` returns an HTML UI resource, not a readable payload. The JSON payload carries `account`, `accounts` (per-account unread counts), `recent_emails` (each row with `was_replied_to` and `has_draft`), `errors`, `error_details` when non-empty, `draft_scan`, and the `unread_count_source` / `unread_count_measured` / `unread_count_note` provenance fields, plus the echoed `include_preview` / `max_total` / `max_per_account` bounds. There is no pinned list and no suggestions list. An empty `recent_emails` is only trustworthy when `errors` is also empty, and `accounts` values are Mail's **cached** unread aggregate, so never report them as exact.
8. **Never target actions by subject or sender.** Each of `move_email`, `update_email_status`, and `manage_trash` returns `TARGET_SELECTOR_DEPRECATED` for `subject_keyword=`, `subject_keywords=`, or `sender=` target selectors. Default path: bounded list/search -> collect `message_id`s -> `move_email(message_ids=[...])` (or the equivalent for status/trash). Date-only or explicit bulk paths still require `allow_filter_scan=True` plus user approval; those scans re-pay mailbox walk cost and are timeout-prone on 24k+ inboxes.
9. **Issue Apple Mail tool calls one at a time, never in parallel.** The server serializes all AppleScript behind a single-flight lock, so concurrent calls only queue and risk timing out; there is no wall-time benefit to firing several tool calls at once. This applies both across separate tool calls in one turn and within any multi-account tool (which now dispatches per-account work sequentially). Wait for each call's result before issuing the next.

## Structured error contract

| Code | When | Remediation |
|------|------|-------------|
| `UNBOUNDED_SCAN_REQUIRED` | Discovery tool called without `recent_days` / `max_emails` bounds | Pass bounded `recent_days` / `max_emails`; page with `offset` for more coverage. `full_inbox_export` is disabled and is never the remediation |
| `UNBOUNDED_EXPORT_DISABLED` | `full_inbox_export` called at all | Use `export_emails` (bounded, max 50/call, page with `offset`), `list_inbox_emails`, or `search_emails` instead |
| `FILTER_SCAN_DISABLED` | Mutation tool called with a date/bulk filter path but without `allow_filter_scan=True` | Follow `remediation.preferred`: `search_emails(...)` or `list_inbox_emails(...)` -> collect `message_id`s -> `tool_name(message_ids=[...])`. Use `remediation.escape_hatch` (`allow_filter_scan=True`) only for user-approved bulk campaigns |
| `TARGET_SELECTOR_DEPRECATED` | Action tool called with subject, sender, or draft-subject target selectors instead of exact ids | Run bounded discovery first, review candidates, then retry with `message_ids=[...]`, `message_id=...`, or `draft_id=...` |
