# Calendar and reply-state closeout - 2026-08-26

## Scope

- Linear: AGENTIC-2527 and AGENTIC-2528.
- Branch: `fix/calendar-read-and-reply-state` from `origin/main` at `79141b1`.
- Release candidate: 3.12.1.

## Completed

- Calendar.app listing recovers stable object-reference IDs without reading the failing `calendarIdentifier` property.
- Bounded reads and writes carry selector kind through the tool boundary; exact-name fallback rechecks live uniqueness and fails closed.
- Explicit Calendar failures remain structured, including access denial, missing or ambiguous targets, EventKit target failure, and malformed counts.
- Calendar deletion resolves a stable ID to a live unique name before the inline delete required by Calendar.app.
- Mail JSON reads preserve the raw native reply flag and add nullable exact Sent-header evidence plus a nullable composite reply state.
- Sent scans are bounded, account-scoped, short-timeout, and honest about errors, truncation, and account caps. Legacy needs-response scanning remains opt-in.
- Primary read/triage docs and bundled pre-draft references describe the new evidence contract and the fact that it does not set Mail's UI answered flag.

## Verification

- Focused Calendar suite: 380 passed before the final delete-path regression test; the final repository count is 2,670 tests.
- Focused reply-state surface: 187 passed before adversarial remediation; cross-cutting compatibility regressions were then rerun green.
- Package Ruff, Ruff format, strict mypy, AppleScript compile/idiom ratchets, module line budget, committed-identity scan, and `git diff --check`: passed.
- Live read-only Calendar: 24 calendars, 24 stable object references, zero enumeration errors; bounded `Work` read returned zero Calendar errors.
- Live FAU Mail fixture: raw native state remained false while exact Sent threading evidence made composite reply state true.
- Live disposable write cycle: calendar create, event create, exact-ID readback, event delete, calendar delete all passed; cleanup confirmed zero acceptance calendars remain.

## Adversarial review

Confirmed findings were fixed: execution-time duplicate-name safety, EventKit fail-closed reads, access-denied preservation, default-calendar ID resolution, recurring-read failure handling, legacy reply-scan compatibility, text-path performance, incomplete-evidence warnings, and account-cap reporting.

Refuted findings: the Calendar object-reference parser handled all live rows; Sent scans are bounded; partial Sent nonmatches remain unknown; unknown rows are not excluded; snapshots do not cross account boundaries; neither read path mutates Mail; no live identity was added to the repository.

## State boundary

Implementation and local/live verification are complete on the feature branch. This is not merged, tagged, released, or promoted to the installed Apple Mail plugin.
