# Public directory listings: handoff

**Date:** 2026-08-24
**Branch:** `chore/public-directory-listings` (base `origin/main` @ `9ba502e`, the v3.11.9 merge)
**Version carried:** 3.12.0
**Companion:** [`submission-packet-2026-08-24.md`](submission-packet-2026-08-24.md) (copy-ready form answers per channel)

## Objective

Make `apple-mail` discoverable inside each client's own plugin browser: Claude Desktop and Cowork "Browse plugins", Claude Code `/plugin` Discover, Cursor's Customize page, and Codex's Plugins tab. A GitHub marketplace URL that users must already know does not meet the bar.

## What is already live (no merge needed)

| Item | Proof |
|------|-------|
| GitHub Release `v3.11.9` with `.mcpb`, `.zip`, `.plugin` | https://github.com/Agentic-Assets/apple-mail-mcp/releases/tag/v3.11.9 |
| 12 repo topics (`mcp`, `mcp-server`, `claude-plugin`, `codex-plugin`, `cursor-plugin`, `apple-mail`, ...) | repository About panel |
| Private vulnerability reporting | repository Security tab |

## What this branch changes

| Area | Change | Why a reviewer needs it |
|------|--------|-------------------------|
| Tool metadata | `title` on all 41 `@mcp.tool` registrations; registry test asserts presence | Plugin browsers and the Claude directory pipeline render titles; tools without them show snake_case names |
| `apple-mail-mcpb/manifest.json` | `manifest_version` 0.3 (was deprecated `dxt_version` 0.1), `compatibility.platforms ["darwin"]`, `compatibility.runtimes.python`, `privacy_policies`, `documentation`, `support`; enforced by `_check_mcpb_directory_contract` | The desktop extension directory rejects manifests missing any of these |
| Cursor | root `.cursor-plugin/marketplace.json`; full `plugin/.cursor-plugin/plugin.json` (`displayName`, `author`, `license`, `homepage`, `repository`, `keywords`, `category`, `logo`, `skills`); `plugin/assets/logo.svg`; `plugin/README.md`; payload classification; `tools/manifest_checks/cursor.py` validation | Cursor reads the root marketplace file for a repository submission and shows logo, author, and README on the listing |
| Policies | `PRIVACY.md`, `SECURITY.md`, README "Privacy Policy" and "Support" sections, bundle README privacy pointer, release-link install steps | Every form asks for a privacy-policy URL and a support channel; the MCPB directory treats a missing policy as an immediate rejection |
| Release files | 3.12.0 in `pyproject.toml`, both plugin manifests, the Cursor manifest, `.claude-plugin/marketplace.json`, `server.json`, the MCPB manifest; CHANGELOG `## 3.12.0 - 2026-08-24` | v3.11.9 is tagged and immutable, so the metadata changes ship as a new version |
| Tasks | this lane folder, `tasks/todo.md` pointer, `tasks/INDEX.md` row | required layout |

Housekeeping performed on the checkout: a pre-staged, untracked-by-history `uv.lock` (2,653 lines, `requires-python = ">=3.10"`) sat at the repository root, where `validate_repo_root.py` and the release gate's no-index-drift check both refuse it. It was unstaged and preserved as a dated backup (`2026-08-24-apple-mail-mcp-uv.lock`) in the parent GitHub folder beside this repository checkout, not deleted; the project uses pip and `.venv/`, has no `uv` configuration, and never committed a lock file. Restore it deliberately, or regenerate with `uv lock`, if it was wanted.

## Verification

Filled in from the gate run on this branch; see the PR body for the full log.

`bash tools/gates/dev-check.sh release` on this branch, 2026-08-24, exit 0:

- committed identity scan: OK; module line budget: OK; tasks layout: OK; repo root: OK
- lint: OK (fatal `ruff check`, `ruff format --check`, `mypy --strict` on `plugin/apple_mail_mcp/`)
- `validate_manifests.sh`: OK (version=3.12.0, tools=41), including the new Cursor
  schema-allowlist checks, root Cursor catalog check, and `_check_mcpb_directory_contract`
- artifacts rebuilt: `apple-mail-plugin.zip` and `apple-mail.plugin` byte-identical
  (15,370,019 bytes), `apple-mail-mcp-v3.12.0.mcpb` (15,388,129 bytes)
- `mcpb` unpack + validate: OK; `claude plugin validate --strict`: OK (manifest at zip
  root, no warnings); offline runtime verified for both the zip and the `.mcpb`
  (fresh venv from the bundled wheelhouse, Python 3.13)
- pytest: full suite green; test count 2462 collected, matches
  `tools/expected_test_count.txt`

`bash tools/gates/source-release-gate.sh` runs on the release commit and stamps it; its
result is recorded in the PR body.

## Channel status

| Channel | Status | Gate |
|---------|--------|------|
| Claude plugin directory (Claude Code `claude-community` marketplace, Cowork and Desktop "Browse plugins" via claude.com/plugins) | Ready to submit after merge | Form at https://platform.claude.com/plugins/submit needs a signed-in Console org (Developer role or higher) |
| Claude Desktop extension directory (`.mcpb`) | Ready to submit after the v3.12.0 release exists | Google Form https://clau.de/desktop-extention-submission, signed-in Google account |
| Cursor Marketplace | Ready to submit after merge | Form https://cursor.com/marketplace/publish, signed-in individual Cursor account; fallback https://cursor.directory/plugins/new |
| Codex / OpenAI Plugins Directory | Blocked by OpenAI policy: only public HTTPS MCP servers are accepted; local stdio plugins are told to wait for local MCP support | Partner contact, or wait; `codex plugin marketplace add Agentic-Assets/apple-mail-mcp` keeps working |
| Open MCP Registry | Blocked on a package-name decision | `server.json` claims PyPI `mcp-apple-mail`, which the upstream author owns (latest 3.2.0); the install-contract validator hard-requires the PyPI package type, so switching to an `.mcpb` reference is a validator change plus a founder call |

The browser extension was not connected in the session that built this branch, so none of the three forms was filed; each needs a signed-in session that only Cayman can provide. Every field value is in the packet.

## Ordered next steps

1. **Cayman:** review PR, then approve the merge ("Cayman approved this merge"). Merging is founder-gated.
2. From clean `main` after the merge: `bash tools/gates/source-release-gate.sh`, `bash tools/gates/create-release-tag.sh --confirm-create`, `git push origin v3.12.0`. Stamp after the merge; the stamp binds HEAD's SHA.
3. `gh release create v3.12.0 --verify-tag --latest` with `apple-mail-mcp-v3.12.0.mcpb`, `apple-mail-plugin.zip`, and `apple-mail.plugin` from that gate run (v3.11.9 followed the same shape).
4. `bash tools/gates/marketplace-handoff.sh v3.12.0`, then the central Marketplace promotion per `docs/marketplace-release-handoff.md`.
5. File the three forms from the packet: Claude plugin directory (repo URL), Claude Desktop extension directory (the v3.12.0 `.mcpb` asset URL), Cursor Marketplace (repo URL). Record submission dates and any ticket IDs here.
6. Decide the PyPI name question; then either publish under an owned name and restore the registry path, or change the `server.json` contract to reference the `.mcpb` asset and publish with `mcp-publisher`.
7. Optional follow-ups a reviewer may raise: pin or vendor the MCP Apps SDK script the dashboard template loads from a CDN; add cleanup for rich-draft `.eml` files under the per-user cache directory.

## Sources checked (2026-08-24)

- https://claude.com/docs/plugins/directory (submission requirements, review pipeline)
- https://claude.com/docs/connectors/building/submission (desktop extension directory, rejection reasons)
- https://github.com/modelcontextprotocol/mcpb/blob/main/MANIFEST.md (manifest 0.3 fields)
- https://developers.openai.com/plugins/deploy/submission and https://developers.openai.com/plugins/guides/submit-claude-plugin (HTTPS-only rule, local stdio guidance)
- https://cursor.com/docs/plugins (root marketplace file, plugin manifest schema) and https://cursor.com/marketplace/publish
- https://registry.modelcontextprotocol.io and https://pypi.org/project/mcp-apple-mail/ (ownership)
