# Thread member completeness closeout (2026-09-07)

**Branch**: `fix/thread-member-completeness`
**Base**: `main` at `73a7532`
**Commits**: the implementation commit `620b26c` (50 files, +3819 / −450), plus two follow-ups at HEAD of the branch: the task-doc correction and the 3.12.2 version bump.
**PR**: https://github.com/Agentic-Assets/apple-mail-mcp/pull/105 — OPEN, MERGEABLE
**Verification**: `tools/gates/dev-check.sh release` passed on the working tree; `tools/gates/source-release-gate.sh` stamped `620b26c`; that stamp binds a commit SHA, so it is superseded by the later branch commits and must be retaken on `main` after the merge. 2757 tests collected, all passing. Live acceptance run against a reachable Exchange account through repo source. GitHub-hosted Actions are disabled in this repo, so there is no CI lane to report.
**State**: PR approved and merging. Tag `v3.12.2` and the GitHub Release follow the merge; no installed-plugin promotion yet.

## Goal

AGENTIC-2794: a conversation's earlier, attachment-bearing member was silently missing from thread enumeration, attachment listing, and thread export, and every completeness signal reported the partial result as whole. Reproduce it, find the root cause, fix it, verify live, and harden the surrounding surface.

## What shipped

One reported symptom decomposed into five root causes across four sub-issues.

- **RC-1 (AGENTIC-2796) `get_email_thread`** — the per-mailbox candidate scan was bounded by `max_messages`, the *return* limit. Now sized from the date window via `compute_scan_upper_bound` with new `THREAD_SCAN_*` bounds, plus a `scan_messages` override. New modules `search/thread_payload.py` (pure-Python payload assembly) and `search/anchor.py` (bounded anchor-mailbox probe).
- **RC-2 (AGENTIC-2798) `search_emails`** — scan sized from the page and `date_from` ignored; ceiling flag compared against the wrong value so the marker never armed. New `search/scan_cap.py`.
- **RC-3 (AGENTIC-2797) `list_email_attachments`** — INBOX-only, and a zero-attachment message was indistinguishable from an unresolved id. Gained `mailbox` / `mailboxes` and full resolution reporting. New `analytics/attachments_helpers.py`.
- **RC-4 `mailbox="All"`** — hard-errored on the anchor fetch. Now resolved through the bounded probe.
- **RC-5 (AGENTIC-2799) `export_emails(scope="thread")`** — hardcoded lookup mailboxes and an unqualified success banner. New `analytics/export_thread_scope.py`.

Docs and skills: `CHANGELOG.md`, `README.md`, `docs/CLAUDE-conventions.md`, a new "Scan bounds are not return bounds" section in `plugin/apple_mail_mcp/tools/CLAUDE.md`, and eight skill files.

## Verification

Local gates only — Actions are disabled here by design. The release-tier gate is re-run after the version bump to 3.12.2, and again on clean `main` after the merge, because the gate stamp binds HEAD's commit SHA rather than its tree.

- `dev-check.sh release`: identity scan, `ruff check`, `ruff format --check`, `mypy --strict` (129 files), module line budget, artifact rebuild, `validate_manifests` (version 3.12.2, tools 41), `mcpb` validate, `claude plugin validate --strict`, tasks layout, repo-root hygiene, offline runtime verified for both artifacts.
- `apple-mail-plugin.zip` and `apple-mail.plugin` byte-identical (sha256).
- 2757 tests (was 2670); 87 added across four new files.
- Live: all six original reproduction steps re-run against the real account through `.venv/bin/python` on repo source. Never through MCP tools, which execute the installed plugin.

## Decisions made

- **The thread scan ceiling (400) is a deliberate exception to `SEARCH_HARD_CEILING` (50).** Measured live, same anchor, only the bound varied: 50 → 6.8 s / 5 members; 150 → 10.9 s / 9; 400 → no worse than 150. Cost is flat past 150, so the ceiling buys completeness at bounded cost. Rejected: keeping the 50 ceiling for consistency, which would have left the bug in place.
- **The `recent_days` date floor is excluded from `thread_incomplete`.** Implemented the other way first and reverted: the floor fires whenever a mailbox holds anything older than the window, so the flag was true on every realistic call. Split into `window_truncated`. Rejected: a single union flag (honest but ignored in practice) and dropping the date-floor report entirely (useful flag, lost information).
- **`max_messages` truncation is also a caller-chosen bound**, so `return_limit_reached` is reported separately rather than folded into `thread_incomplete`. Same reasoning as the date floor.
- **Marker rows travel on their own prefixes, never as extra record fields.** `_parse_search_records` splits with `split("|||", 14)`, so a 16th field would fold into `was_replied_to`; and left in the stream, a short marker row is silently *dropped* rather than mis-parsed. Hence `split_thread_markers` runs first.
- **No shared marker-parsing primitive was extracted** across the three parsers. The only common step is one line each, and a shared entry point would create the exact risk of a marker row reaching the record parser.
- **Corrected mid-flight: this is 3.12.2, not an addition to 3.12.1.** The first pass filed the notes under the existing `## 3.12.1 - 2026-08-26` section on the belief that 3.12.1 was still open. It was not: the tag exists at `73a7532` and the Release went out 2026-08-28. Appending there would have made the *published* CHANGELOG for v3.12.1 describe five root-cause fixes that the v3.12.1 tag does not contain, so anyone installing that tag would read release notes for code they do not have. Checking for the tag and the Release, not just for an `## Unreleased` heading, is what distinguishes an open lane from a shipped one.
- **The attachment-count probe binds its error variable** rather than using a bare sentinel arm, so it needed no bare-try ratchet allowance. Preferred over bumping the baseline.

## Things deliberately deferred

- `include_sent=False` bounds the export candidates but not the thread scan, so a Sent-resident member can be enumerated and then dropped.
- Text mode reports `FOUND 0` when the anchor was recovered: the banner renders inside AppleScript while anchor retention happens in Python. JSON mode is correct; skills now direct completeness checks to JSON.
- `thread.py`'s ceiling detection reads `count of messages` first, which is a known stale-prone property, rather than slicing optimistically and recovering on error the way `script.py` does. Pre-existing shape, preserved.

## Left to the operator

- **Merge approval for PR #105 was given by the founder**, which is what unblocks the merge; passing every gate never substituted for it.
- **Resolved: this carries its own version, tag, and release.** The earlier assumption that 3.12.1 was an open lane was wrong. `v3.12.1` is tagged at `73a7532` and was published as a GitHub Release on 2026-08-28, so it is shipped, not open. The version is bumped to 3.12.2 across `pyproject.toml`, the three plugin manifests, `.claude-plugin/marketplace.json`, `server.json`, and `apple-mail-mcpb/manifest.json`; the notes moved to a new `## 3.12.2 - 2026-09-07` CHANGELOG section and the `## 3.12.1` section was restored to byte-match the tag. Closing out is merge → source release gate on `main` → signed tag `v3.12.2` → GitHub Release with all three artifacts.
- Installed-plugin promotion, which is a separate marketplace step.
